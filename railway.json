{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "nixpacks",
      "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable /flutter && export PATH=\"/flutter/bin:$PATH\" && flutter config --enable-web && flutter pub get && flutter build web"
    },
    "deploy": {
      "startCommand": "cp -r build/web /app && serve -s /app",
      "healthcheckPath": "/",
      "restartPolicyType": "on_failure"
    }
  }
  